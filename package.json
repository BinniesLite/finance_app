{
  "name": "finance_app",
  "version": "1",
  "description": "Finance app ",
  "main": "none ",
  "repository": "https://github.com/BinniesLite/finance_app.git",
  "author": "BinniesLite <89041460+BinniesLite@users.noreply.github.com>",
  "license": "MIT",
  "scripts": {
    "build:client": "cd client && yarn install && yarn run build",
    "build:server": "cd server && yarn install && yarn run build",
    "deploy": "yarn run build:client && yarn run build:server"
  }
}
